services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
